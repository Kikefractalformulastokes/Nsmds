# Next Repo/Access Request

Repos/folders still needed to move the rest of the CEOS brief from plan to build. None of these
are in this session's scope (`kikefractalformulastokes/nsmds` only). Grant via `add_repo` (GitHub
repos) or a session already scoped to them.

| Repo/property | Why it's needed | Priority |
|---|---|---|
| CoreSyn (web) | Main company site — `/technology`, `/cep`, `/materials`, `/aerospace` pages all depend on this existing | High |
| CEOS | The actual internal OS — nothing in `03_CEOS/` could be restored/patched without it | High |
| ModelAssuranceLab | Enterprise product site — copy alignment task is already logged and waiting | Medium |
| Materials | First internal scientific vertical — currently 100% placeholder | Medium |
| Aerospace / Airbus PoC (dedicated repo, if one exists separately from CoreSyn) | Strategic priority per the brief; needed to build the private PoC documents in `06_AIRBUS_POC_LAB/` | High |
| RiesgoDeObra | **Partially covered** — external QA was relayed via Enrique/Chrome, but CODE has never directly accessed the repo/site. Direct access would let CODE close the remaining transactional/verification gaps itself instead of relaying through Chrome for every check. | Medium (not fully accessible; Chrome-relayed evidence exists) |
| `mathematical_audit` | Referenced in the original brief's repo list; not touched at all this session | Low — only if review work on it continues |
| `external_review_pack` | Referenced in the brief for Materials external review; not touched at all | Low — only if Materials review work continues |

## Also needed (not repo access, but blocking non-repo work)

- Azure account access (for `16_SECURITY_GROWTH/02_AZURE_CREDITS/`)
- Stripe dashboard, Tally dashboard, Search Console, DNS/registrar logins (for Chrome agent)
- Analytics tool decision (Enrique's choice, not a repo)

No request here implies approval to spend, deploy, or change anything — access alone unblocks
planning and QA; every subsequent action still follows the same approval rules as this session.
