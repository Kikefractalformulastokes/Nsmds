# CRM — Ready for Import

**Status: SCHEMA READY, NO DATA.** `LEAD_PIPELINE.csv` has headers only — zero rows. No real
lead has been recorded anywhere in this command center. Any future import must go through this
schema and must carry a `consent_source` value for every row — no lead without a documented
consent/source.
