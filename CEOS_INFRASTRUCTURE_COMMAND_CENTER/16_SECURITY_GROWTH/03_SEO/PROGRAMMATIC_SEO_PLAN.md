# Programmatic SEO Plan

**Status: NOT STARTED — deliberately deferred.**

Per `SEO_MASTER_PLAN.md` sequencing, programmatic SEO (templated pages at scale, e.g. per-region
RiesgoDeObra landing pages, per-material-class Materials pages) should only begin after:

1. At least one manually-built page per cluster is live and has demonstrated intent match
   (bounce rate, time on page, or lead conversion — needs `ANALYTICS_MASTER_PLAN.md` tracking
   live first).
2. A clean, reusable page template exists (avoids thin/duplicate-content risk).
3. A real keyword list is validated (not the candidate list in `KEYWORD_MAP.md`).

## Candidate future programmatic sets (not yet built)

- RiesgoDeObra: per-province or per-defect-type landing pages ("revisión de obra en [ciudad]").
- Materials: per-material-class evidence pages.

No template or page has been generated. This file is a placeholder until the prerequisites above
are met.
