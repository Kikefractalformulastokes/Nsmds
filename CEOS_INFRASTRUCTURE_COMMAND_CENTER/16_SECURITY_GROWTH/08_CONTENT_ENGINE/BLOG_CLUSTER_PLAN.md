# Blog Cluster Plan

Mirrors `03_SEO/CONTENT_CLUSTER_MAP.md` — same pillar/supporting structure, blog-specific
publishing notes:

- Each pillar page (e.g. `/technology`, RiesgoDeObra home, `/aerospace`, `/materials`) anchors a
  blog cluster.
- Supporting posts link back to the pillar and to a conversion CTA (per `CTA_MAP.md`).
- Publishing cadence follows `EDITORIAL_CALENDAR_30_DAYS.md` once dates are set.

**Status:** no blog exists in any accessible property yet.
