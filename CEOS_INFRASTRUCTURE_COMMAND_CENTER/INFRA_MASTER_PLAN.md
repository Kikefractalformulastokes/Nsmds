# CoreSyn / CEOS Infrastructure — Master Plan

**Status:** DRAFT — SCOPE-LIMITED AUDIT
**Date:** 2026-07-22
**Executed by:** Claude Code, session scoped to GitHub repo `kikefractalformulastokes/nsmds` only.

## 0. Critical scope disclosure (read first)

This session's GitHub access is restricted to a single repository:

- `kikefractalformulastokes/nsmds` (branch `claude/coresyn-ceos-infrastructure-audit-sthqhh`)

The brief that triggered this command center references a much larger estate — CoreSyn web,
CEOS, ModelAssuranceLab, Airbus PoC Lab, Materials, RiesgoDeObra, `coresyn.io`, `app.coresyn.io`,
`riesgodeobra.es`, Stripe, Tally, Search Console, DNS. **None of those repos, domains, or
accounts are reachable from this session.** No such repos exist under this session's connected
GitHub sources, and this environment has no browser/DNS/Stripe/Tally/Analytics account access.

Per the governing rules ("no improvises", "no mezcles proyectos", "no toques secretos"), this
plan does **not** invent structure, copy, numbers, or status for systems it cannot see. Every
section below is either:
- **AUDITED** — based on directly inspected files in `nsmds`, or
- **NOT_ACCESSIBLE_THIS_SESSION** — flagged explicitly, with the access needed to unblock it.

## 1. What this session actually did

1. Read-only audit of the `nsmds` repository (only repo in scope).
2. Built `CEOS_INFRASTRUCTURE_COMMAND_CENTER/` with the requested folder tree and documents.
3. Filled every document with real findings where evidence exists, and an explicit
   `NOT_ACCESSIBLE_THIS_SESSION` marker everywhere it doesn't.
4. Did not modify `index.html`, `README.md`, or any file under `research/` (production/public
   assets) — findings on those are reported, not silently fixed.
5. Did not create accounts, touch DNS, install analytics, or deploy anything.

## 2. Company model kept consistent with the brief

- CoreSyn = compañía principal.
- CEOS = sistema operativo interno (not found in this repo).
- NS-MDS = origen científico / investigación avanzada — **this is what `nsmds` actually is.**
- Assurance Core / CEP = capa de evidencia (not found in this repo).
- Materials = primer vertical científico interno (not found in this repo).
- Airbus PoC Lab = prioridad estratégica (referenced only as a demo label in `index.html`,
  see `06_AIRBUS_POC_LAB/`).
- RiesgoDeObra = experimento comercial activo (not found in this repo).

No new company (e.g. "Laminar") was created. No venture conversion of AuditFlow was performed —
neither is referenced anywhere in this repo.

## 3. Next unlock

See `15_CHROME_HANDOFF/NEXT_ACTIONS_FOR_CHROME.md` and the root
`SECRETS_REQUIRED_BUT_NOT_STORED.md` for exactly what access/credentials would let a future
session complete the remaining 90% of this brief (CoreSyn web, CEOS, Materials, RiesgoDeObra,
Aerospace deck, SEO/DNS work).
