# CoreSyn Implementation Plan

**Status: BLOCKED — no CoreSyn repo/site in scope.**

1. Confirm whether a CoreSyn web property already exists (repo, hosting) before building
   anything new — per the brief's own rule ("No implementar sin revisar si la web actual existe y
   dónde"). This session could not check.
2. Once confirmed: get repo access, audit the existing site structure against
   `CORESYN_PAGE_STRUCTURE.md`.
3. Get Enrique's positioning copy for `CORESYN_COPY_v1.md`.
4. Build/update pages incrementally, small PRs, staging before production.
5. Run `CLAIMS_QA_RULES.md` on every page before publish.
