# 01_REPOS

Per-repo detail folder. Only `nsmds` is populated because it's the only repo in scope this
session — see `NSMDS_REPO_NOTES.md`. Once other repos are granted access, this folder gets one
subfolder per repo with its own inventory detail, mirroring `00_INVENTORY/REPO_INVENTORY.md`.
