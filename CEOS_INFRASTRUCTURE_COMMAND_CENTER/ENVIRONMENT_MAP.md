# Environment Map

| Environment | Exists? | Notes |
|---|---|---|
| Production (`nsmds`) | Presumed — this repo's `main` branch, unconfirmed public URL | No staging branch/environment found |
| Staging (`nsmds`) | Not found | No `staging` branch, no preview config |
| Production (all other properties) | Unknown | NOT_ACCESSIBLE_THIS_SESSION |

**Rule enforced this session:** no production file was edited (rule: "staging first, never
production directly if staging exists" — since no staging exists either, no production edits
were made at all pending explicit approval).
